      <link rel="stylesheet" href="https://unicons.iconscout.com/release/v3.0.6/css/line.css">
      <!--inicio espaço superior da tag desconto-->      
      <style>        
      .product-meta__title {    
       margin-bottom:   {{ section.settings.espaco_titulo}};
       }             
      .product-meta__label-list {       
       padding-top: {{ section.settings.superior_tag_espaco_promocao}};
       padding-bottom: {{ section.settings.inferior_tag_espaco_promocao}}       
       }        
       </style>            
       <!--final espaço da tag sale-->          
       <!--Inicio sombra variantes-->     
       {% if section.settings.variante_sombra %}         
       <style>
      .variant-swatch__item {        
       box-shadow:1px 4px 4px #dbdbdb;
       {% if section.settings.variante_redonda %}  
       border-radius: 30px;
       {%- else-%}  
       border-radius: 7px; 
       {%- endif -%} 
       }  
      .variant-swatch__radio:checked + .variant-swatch__item {
       {% if section.settings.variante_redonda %}  
       border-radius: 30px;
       {%- else-%}  
       border-radius: 7px; 
       {%- endif -%} 
       }      
       @media screen and (min-width: 641px) {
      .product-form__option-name {
       margin-bottom: 7px;
       margin-top: -{{ section.settings.margin_superior_variante}}px; 
       }
       }        
      </style>      
      {%- endif -%}
      <style>    
       /*tamanho do titulo desktop*/ 
          
    
      @media screen and (min-width: 641px) {
      .h1, .rte h1 {
      font-size:  {{ section.settings.titulo_tamanho_desktop}};
      line-height: 1.43;
      font-weight: {{ section.settings.titulo_peso_desktop}};
      } 
      }      
      /*Tamanho do titulo mobile*/              
      @media screen and (max-width: 640px) {          
     .h1, .rte h1 {
      margin-bottom: 2px;
      font-size: {{ section.settings.titulo_tamanho_mobile}};
      line-height: 1.45;
      font-weight: {{ section.settings.titulo_peso_mobile}};
      }
      } 
      </style>    
      <!-- estilo fonte para mobile e desktop-->       
      <style>             
      .price--highlight { 
       color:{{section.settings.cor_price_cor}}; 
       font-weight:{{section.settings.peso_price_text}};       
       }
       /*desktop*/
       @media screen and (min-width: 640px) {        
      .price{        
       font-size: {{section.settings.preco_tamanho_desktop}};
       color: {{section.settings.cor_price_cor}};
       font-weight: {{section.settings.peso_price_text}};
       }            
      .price--compare {
       font-size: 16px; 
       margin-left: 10px;
       text-decoration: line-through;
       }
       }    
      /*mobile*/
      @media screen and (max-width: 640px) {
      .price{        
       font-size: {{section.settings.preco_tamanho_mobile}};
       font-weight: bold;  
       }      
       }
      .price--compare {
       font-size: 16px; 
       margin-left: 0px;
       text-decoration: line-through;
       font-weight: normal;       
       }          
       </style>   
       <!-- Final estilo fonte para mobile e desktop-->       